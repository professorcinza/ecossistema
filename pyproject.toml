[project]
name = "teia-kernel"
version = "1.0.0"
description = "TEIA Kernel — system prompts and analytical constitution"
authors = [{name = "Cleiton Moura"}]
license = "MIT"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
